name: Keep Streamlit App Awake

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit
        run: |
          curl -L --fail --retry 5 --retry-all-errors \
          https://mhf-civil-calc.streamlit.app/
